export const BASE_URL = "chat server ip";
export const APP_TOKEN = "chat_token";
export const PORT = 3333;

export const NAME_SPACE = "chat";

export const URL = `${BASE_URL}:${PORT}/${NAME_SPACE}`;

export const CHAT_API_URL = `${BASE_URL}:${PORT}`;

export const CHAT_API_GET_THREAD = `/chatroom/messagethread`;
export const CHAT_API_GET_MESSAGES = `/chatroom/messages`;

export const API_TIMEOUT = 30000;
